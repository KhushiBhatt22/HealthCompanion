<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/signup"
    tools:context=".MotivationActivity">

    <!-- Medal Image -->

    <!-- Medal Name -->

    <!-- Points -->

    <TextView
        android:id="@+id/tvMotivationTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="43dp"
        android:layout_marginBottom="84dp"
        android:text="Your Progress"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/imgMedal"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/imgMedal"
        android:layout_width="128dp"
        android:layout_height="0dp"
        android:layout_marginBottom="56dp"
        android:contentDescription="@string/medal_image"
        android:src="@drawable/ic_medal_bronze"
        app:layout_constraintBottom_toTopOf="@+id/tvMedalName"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tvMotivationTitle" />

    <TextView
        android:id="@+id/tvMedalName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="9dp"
        android:layout_marginBottom="38dp"
        android:text="Bronze Medal"
        android:textSize="18sp"
        app:layout_constraintBottom_toTopOf="@+id/tvPoints"
        app:layout_constraintStart_toStartOf="@+id/imgMedal"
        app:layout_constraintTop_toBottomOf="@+id/imgMedal" />

    <TextView
        android:id="@+id/tvPoints"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="15dp"
        android:layout_marginBottom="491dp"
        android:text="Points: 0"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@+id/tvMedalName"
        app:layout_constraintTop_toBottomOf="@+id/tvMedalName" />
</androidx.constraintlayout.widget.ConstraintLayout>